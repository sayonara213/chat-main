import {Container} from "./components/container/container";
import {Theme} from "./constants/theme";

function App() {
  return (
      <Theme>
        <Container/>
      </Theme>
  );
}

export default App;
